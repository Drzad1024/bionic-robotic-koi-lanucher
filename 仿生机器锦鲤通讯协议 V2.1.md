> **版本**：V2.2
> **更新日期**：2026-03-02
> **适用硬件**：V2.15 (STM32主控)

## 1. 物理层参数

* **通讯接口**：UART
* **波特率**：115200 bps（设置参数时为 9600）
* **数据位**：8
* **停止位**：1
* **校验位**：None (无)
* **发射功率**: 22 dBm
* **传输速率**：62.5 kbps
* **最大发送频率**：25 Hz


## 2. 帧结构定义

采用变长帧结构，总长度 **9 ~ N** 字节。

| 字节偏移    | 字段名       | 长度 | 说明                                                         |
| ----------- | ------------ | ---- | ------------------------------------------------------------ |
| **0~1**     | **Header**   | 2    | 帧头，固定为 `0xAA66`                                        |
| **2~3**     | **SenderID** | 2    | 发送方 ID （大端模式）                                       |
| **4~5**     | **Password** | 2    | 配对密码 / 控制口令 （大端模式）                             |
| **6**       | **LEN**      | 1    | 后续数据长度 = `sizeof(CMD) + sizeof(Payload) + sizeof(XOR)` |
| **7**       | **CMD**      | 1    | 功能指令码                                                   |
| **8 ~ N-1** | **PAYLOAD**  | 0~M  | 变长数据域 (参数)                                            |
| **N**       | **XOR**      | 1    | 校验码：从 Byte 0 到 Byte N-1 的异或值                       |

> **校验算法**：`XOR = Byte[0] ^ Byte[1] ^ ... ^ Byte[N-1]`

> **鉴权说明**：
> * 上电后进入配对模式，可通过搜索指令 `0xBA` 寻找设备，此时不需要鉴权，任何上位机发送均可响应。
> * 上位机发送配对密码和控制口令进行鉴权，鉴权成功后进入正常工作模式。
> * 工作模式下，所有控制指令必须携带正确的控制口令，不再使用配对密码。

## 3. 指令集详情

上位机发送给下位机的控制指令。

### 3.1 配对操作 (0x90)

#### **0x90 - 配对指令**

> **功能**：使用配对密码进行设备配对，并附带后续的控制口令，成功后进入正常工作模式。

**Payload 结构 (4 Bytes)**

| Byte 0         | Byte 1         | Byte 2         | Byte 3         |
| -------------- | -------------- | -------------- | -------------- |
| **Password_H** | **Password_L** | **CtrlCode_H** | **CtrlCode_L** |

* **Password**: 配对密码（大端），默认 `0x0000`。
* **CtrlCode**: 配对成功后的控制口令（大端），后续控制指令需携带此口令进行鉴权。
* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 06 90 00 00 12 34 94` 表示使用密码 `0x0000` 配对，成功后控制口令为 `0x1234`。

#### **0x91 - 取消配对**

> **功能**：取消当前配对，恢复到配对模式。

---

### 3.2 运动控制 (0xA*)

#### **0xA0 - 停止并复位**

> **功能**：停止游动，舵机回中，恢复直线姿态。

**Payload 结构 (1 Byte)**

| Byte 0   |
| -------- |
| **Boot** |

* **Boot**: 是否作为开机参数保存 (`1`=保存, `0`=不保存)
* **发送示例** (ID=0x7890): `AA 66 78 90 03 A0 00 87`

#### **0xA1 - CPG 游动控制**

> **功能**：设定机器鱼游动的CPG参数。

**Payload 结构 (7 Bytes)**

| Byte 0   | Byte 1    | Byte 2    | Byte 3     | Byte 4     | Byte 5       | Byte 6   |
| -------- | --------- | --------- | ---------- | ---------- | ------------ | -------- |
| **Freq** | **Amp_H** | **Amp_L** | **Bias_H** | **Bias_L** | **Interval** | **Boot** |

**参数说明**

| 参数         | 类型     | 单位/范围 | 说明                                            |
| ------------ | -------- | --------- | ----------------------------------------------- |
| **Freq**     | `uint8`  | 0.1 Hz    | 频率。例：`15` = 1.5 Hz (Max 4Hz)               |
| **Amp**      | `uint16` | 0.1 度    | 摆幅。例：`200` = 20.0° (Max 38°)               |
| **Bias**     | `int16`  | 0.1 度    | 偏置 (有符号)。例：`-50` = 左偏 5.0° (Max ±38°) |
| **Interval** | `uint8`  | Bool      | 间歇模式开关。`1`=开，`0`=关                    |
| **Boot**     | `uint8`  | Bool      | 是否作为开机参数保存。`1`=保存，`0`=不保存      |

* **发送示例** (ID=0xC32B):  `AA 66 C3 2B 00 00 09 A1 14 00 C8 00 32 00 00 62` 表示设置频率2.0Hz，摆幅20.0度，偏置+5.0度，关闭间歇模式，不保存为开机参数。

#### **0xA2 - 间歇参数设置**

> **功能**：配置间歇游动的周期和滑行时间。

**Payload 结构 (4 Bytes)**

| Byte 0      | Byte 1      | Byte 2      | Byte 3   |
| ----------- | ----------- | ----------- | -------- |
| **N_Burst** | **Coast_H** | **Coast_L** | **Boot** |

**参数说明**

| 参数        | 类型     | 说明                                       |
| ----------- | -------- | ------------------------------------------ |
| **N_Burst** | `uint8`  | 摆动周期数 (次)                            |
| **T_Coast** | `uint16` | 滑行时间 (单位: 0.1s)。例：`20` = 2.0秒    |
| **Boot**    | `uint8`  | 是否作为开机参数保存。`1`=保存，`0`=不保存 |

* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 06 A2 05 00 14 00 91` 表示设置间歇为 5 次，滑行 2.0 秒，不保存为开机参数。

#### **0xA3 - 位置控制 (舵机)**

> **功能**：直接控制舵机角度，注意切勿超出实际机械范围。

**Payload 结构 (4 Bytes)**

| Byte 0     | Byte 1     | Byte 2     | Byte 3     |
| ---------- | ---------- | ---------- | ---------- |
| **Ang1_H** | **Ang1_L** | **Ang2_H** | **Ang2_L** |

**参数说明**

| 参数       | 类型     | 说明                                                   |
| ---------- | -------- | ------------------------------------------------------ |
| **Angle1** | `uint16` | S1 角度 (0.1度)，90度为中位，0~180度，`0xFFFF`为不修改 |
| **Angle2** | `uint16` | S2 角度 (0.1度)，90度为中位，0~180度，`0xFFFF`为不修改 |

#### **0xA4 - 表演模式**

> **功能**：执行预设的动作序列。

**Payload 结构 (2 Byte)**

| Byte 0         | Byte 1   |
| -------------- | -------- |
| **Mode_Index** | **Boot** |

**参数说明**
| 参数           | 类型    | 说明                                       |
| -------------- | ------- | ------------------------------------------ |
| **Mode_Index** | `uint8` | 表演序号 (0~255)。`0` 为停止表演并复位。   |
| **Boot**       | `uint8` | 是否作为开机参数保存。`1`=保存，`0`=不保存 |

* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 04 A4 01 00 85` 表示开始执行表演模式1，不保存为开机参数。
* 
#### **0xA5 - 档位模式**

> **功能**：手柄常用的档位控制。

**Payload 结构 (3 Bytes)**

| Byte 0        | Byte 1       | Byte 2   |
| ------------- | ------------ | -------- |
| **SpeedGear** | **TurnGear** | **Boot** |

**参数说明**

| 参数          | 类型    | 说明                                                |
| ------------- | ------- | --------------------------------------------------- |
| **SpeedGear** | `uint8` | 速度档位 (0-15)                                     |
| **TurnGear**  | `uint8` | 转向档位 (0-15)。7为直行，0为左转满档，15为右转满档 |
| **Boot**      | `uint8` | 是否作为开机参数保存。`1`=保存，`0`=不保存          |

* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 05 A5 08 07 00 8E` 表示设置速度档位8，转向直行，不保存为开机参数。

#### **0xA6 - 电调速度控制**

> **功能**：控制电调（ESC）速度与方向。

**Payload 结构 (2 Bytes)**

| Byte 0      | Byte 1   |
| ----------- | -------- |
| **ESC_Spd** | **Boot** |

**参数说明**

| 参数        | 类型    | 说明                                                                             |
| ----------- | ------- | -------------------------------------------------------------------------------- |
| **ESC_Spd** | `int8`  | 电调速度，范围 `-100 ~ +100`；负数=反转，0=停止，正数=正转，绝对值表示速度百分比 |
| **Boot**    | `uint8` | 是否作为开机参数保存。`1`=保存，`0`=不保存                                       |

* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 04 A6 32 00 B9` 表示电调正转 50%，不保存为开机参数。

---

### 3.3 系统配置 (0xB*)

#### **0xB0 - 消息回复设置**

> **功能**：设置无指令超时时间及回复开关。

**Payload 结构 (3 Bytes)**

| Byte 0     | Byte 1     | Byte 2     |
| ---------- | ---------- | ---------- |
| **Time_H** | **Time_L** | **Enable** |

* **Time**: 无指令超时时间 (单位：秒)
* **Enable**: 回复开关 (`1`=不回复，`0`=回复)
* **发送示例** (ID=0xC32B):  `AA 66 C3 2B 00 00 05 B0 00 1E 00 9D` 表示设置无指令超时30秒，开启回复功能。 
* **发送示例** (ID=0xC32B):  `AA 66 C3 2B 00 00 05 B0 00 1E 01 9C` 表示设置无指令超时30秒，关闭回复功能。

#### **0xB1 - LED 控制**

> **功能**：设置 LED 灯光模式。一般不建议修改。

**Payload 结构 (1 Bytes)**

| Byte 0   |
| -------- |
| **Mode** |

* **Mode**: `0`=灭, `1`=亮, `2`=慢闪, `3`=快闪, `4`=心跳


#### **0xB5 - 舵机电源控制**

> **功能**：独立控制舵机供电。

**Payload 结构 (1 Bytes)**

| Byte 0   |
| -------- |
| **Flag** |

**参数说明**

| 参数     | 类型    | 说明                                           |
| -------- | ------- | ---------------------------------------------- |
| **Flag** | `uint8` | `Bit0`=S1开关, `Bit1`=S2开关 (`1`=ON, `0`=OFF) |

#### **0xB6 - 自动回传状态设置**

> **功能**：设置自动回传状态的开关

**Payload 结构 (3 Bytes)**
| Byte 0     | Byte 1  | Byte 2     |
| ---------- | ------- | ---------- |
| **Enable** | **CMD** | **Period** |

**参数说明**
| 参数       | 类型    | 说明                                                                                   |
| ---------- | ------- | -------------------------------------------------------------------------------------- |
| **Enable** | `uint8` | 自动回传状态开关 (`1`=开启, `0`=关闭)                                                  |
| **CMD**    | `uint8` | 回传状态指令码 (参见3.4节，可选 `0xC1/0xC2/0xC6`)                                      |
| **Period** | `uint8` | 回传周期 (单位: 秒)，`0` 表示仅在状态变化时回传（不支持变化触发的项按周期最小1秒执行） |

* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 05 B6 01 C1 05 52` 表示开启自动回传系统状态，周期5秒。
* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 05 B6 01 C6 01 55` 表示开启自动回传IMU状态，周期1秒。

#### **0xBA - 搜索设备**

> **功能**：广播搜索设备，任意 ID 均可响应
> 
**Payload 结构**：无

* **发送示例** (ID=0xC32B): `AA 66 C3 2B 00 00 02 BA 9C` 表示广播搜索设备指令。

---

### 3.4 查询与传感器 (0xC*)

> **注意**：发送查询指令时 Payload 通常为空，下位机返回对应的 0xF* 帧。

| CMD      | 名称                     | 返回帧   | 说明                                                 |
| -------- | ------------------------ | -------- | ---------------------------------------------------- |
| **0xC1** | 查询系统状态             | **0xF1** | 返回电量、实时功率、运行时间、续航估计时间、运行模式 |
| **0xC2** | 查询舵机状态             | **0xF2** | 返回舵机输出状态、电流、舵机故障标志                 |
| **0xC3** | 查询开机参数与固件版本号 | **0xF3** | 返回开机参数与固件版本号                             |
| **0xC4** | 查询Flash数据            | **0xF4** | 返回指定Flash地址的数据                              |
| **0xC6** | 查询六轴IMU              | **0xF6** | 返回三轴加速度与三轴角速度（均为有符号16位）         |
| **0xC7** | 查询姿态数据             | **0xF7** | 返回姿态数据（计算后结果）                           |
| **0xC8** | 查询深度数据             | **0xF8** | 返回深度数据（计算后结果）                           |

---

### 3.5 开机参数 (0xD*)

> 此类指令用于将当前配置写入 Flash，掉电保存。

| CMD      | 名称               | Payload 结构                                                           | 说明                                                                    |
| -------- | ------------------ | ---------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| **0xD0** | **恢复出厂设置**   | 无                                                                     | 重置所有参数                                                            |
| **0xD1** | 设置开机参数       | `Mode` `ReplyEn` `Bias1_H` `Bias1_L` `Bias2_H` `Bias2_L` `OCTime` (7B) | Mode:0~7，ReplyEn:0开启/1关闭，Bias范围-900~900，OCTime单位秒           |
| **0xD4** | 设置查询 Flash     | `Offset` (1B)                                                          | 读取指定 Flash 地址数据，范围0-24（包括24），**修改后强制开启消息回复** |
| **0xD6** | 修改FishID         | `ID_H` `ID_L` (2B)                                                     | **谨慎修改**，修改设备 FishID，**修改后强制开启消息回复**               |
| **0xD7** | 修改密码           | `Pwd_H` `Pwd_L` (2B)                                                   | 修改配对密码，**修改后强制开启消息回复**                                |
| **0xD8** | 修改Lora信道       | `CH` (1B)                                                              | 修改LoRa信道，0~83，对应频率410~493MHz，**修改后强制开启消息回复**      |
| **0xD9** | 复位舵机损坏标志位 | 无                                                                     | 清除舵机过流保护标志位，请确认舵机无损坏再复位，否则可能损坏控制板      |

## 4. 状态返回帧（0xF*）

下位机回复给上位机的数据帧。此时返回密码均为 `0x0000`。

### 4.1 0xFF - 通用指令应答

**Payload 结构（2 Bytes）**

| Byte 0         | Byte 1     |
| -------------- | ---------- |
| **Source_CMD** | **Status** |

**参数说明**
| 参数           | 类型    | 说明                        |
| -------------- | ------- | --------------------------- |
| **Source_CMD** | `uint8` | 源指令码 (被回复的指令 CMD) |
| **Status**     | `uint8` | 状态码 (参见附录错误代码表) |

---

### 4.2 0xF1 - 运行状态 / 电压 / 功率 / 电量 / 时间
用于 UI 界面自动刷新的核心状态，包含运行状态 / 电压 / 功率 / 电量 / 时间

**Payload 结构（7 Bytes）**

| Byte 0     | Byte 1        | Byte 2        | Byte 3      | Byte 4      | Byte 5      | Byte 6      |
| ---------- | ------------- | ------------- | ----------- | ----------- | ----------- | ----------- |
| **BatPct** | **Voltage_H** | **Voltage_L** | **Power_H** | **Power_L** | **EstTime** | **RunMode** |

**参数说明**

| 参数        | 类型     | 说明                                                                         |
| ----------- | -------- | ---------------------------------------------------------------------------- |
| **BatPct**  | `uint8`  | 电池电量百分比 (0~100)                                                       |
| **Voltage** | `uint16` | 电池电压 (单位: 0.01V)，例：`823` = 8.23V                                    |
| **Power**   | `uint16` | 实时功率 (单位: mW)，例：`1500` = 1500mW                                     |
| **EstTime** | `uint8`  | 续航估计时间 (单位: 10分钟)，例：`12` = 120分钟 = 2小时                      |
| **RunMode** | `uint8`  | 当前运行模式 (0=停止，1=CPG，2=CPG+间歇，3=舵机位置，4=表演，5=档位，7=保护) |

* **返回示例** (ID=0xC32B): `AA 66 C3 2B 09 F1 00 03 47 04 B9 00 00 25` 表示电量0%，电压8.39V，功率1209mW，续航0分钟，运行模式停止。

---

### 4.3 0xF2 - 舵机状态数据
包含舵机输出状态与电流信息。

**Payload 结构（6 Bytes）**

| Byte 0   | Byte 1     | Byte 2     | Byte 3     | Byte 4     | Byte 5   |
| -------- | ---------- | ---------- | ---------- | ---------- | -------- |
| ServoOUT | Current1_H | Current1_L | Current2_H | Current2_L | ServoBad |

**参数说明**
| 参数         | 类型     | 说明                                             |
| ------------ | -------- | ------------------------------------------------ |
| **ServoOUT** | `uint8`  | 舵机电源输出状态，0关闭，1开启，Bit0=S1，Bit1=S2 |
| **Current1** | `uint16` | 舵机1 电流 (单位: mA)，例：`500` = 500mA         |
| **Current2** | `uint16` | 舵机2 电流 (单位: mA)，例：`500` = 500mA         |
| **ServoBad** | `uint8`  | 舵机损坏标志位，0正常，1异常，Bit0=S1, Bit1=S2   |

* **返回示例** (ID=0xC32B): `AA 66 C3 2B 08 F2 00 00 00 00 00 00 D2` 表示舵机均关闭，电流0mA，舵机状态正常。
---

### 4.4 0xF3 - 开机参数与固件版本号回传
用于回复开机参数与固件版本号查询请求（`0xC3`）。

**Payload 结构（9 Bytes）**
| Byte 0       | Byte 1      | Byte 2      | Byte 3      | Byte 4      | Byte 5      | Byte 6     | Byte 7      | Byte 8     |
| ------------ | ----------- | ----------- | ----------- | ----------- | ----------- | ---------- | ----------- | ---------- |
| **BootMode** | **ReplyEn** | **Bias1_H** | **Bias1_L** | **Bias2_H** | **Bias2_L** | **OCTime** | **TCP_Ver** | **SW_Ver** |

**参数说明**
| 参数         | 类型    | 说明                                                                     |
| ------------ | ------- | ------------------------------------------------------------------------ |
| **BootMode** | `uint8` | 开机模式 (0=停止，1=CPG，2=CPG+间歇，3=舵机位置，4=表演，5=档位，7=保护) |
| **ReplyEn**  | `uint8` | 回复使能 (1=关闭回复, 0=开启回复)                                        |
| **Bias1**    | `int16` | 舵机1 机械安装偏差 (单位: 0.1度)                                         |
| **Bias2**    | `int16` | 舵机2 机械安装偏差 (单位: 0.1度)                                         |
| **OCTime**   | `uint8` | 过流维持超时时间，单位秒，超时后关闭输出，默认为2秒                      |
| **TCP_Ver**  | `uint8` | 通讯协议版本号，`0x22` 代表 V2.2                                         |
| **SW_Ver**   | `uint8` | 固件软件版本号，`0x22` 代表 V2.2                                         |

* **返回示例** (ID=0xC32B): `AA 66 C3 2B 0B F3 00 00 00 00 00 00 00 22 22 DE` 表示开机模式停止，回复使能开启，舵机偏差0，过流超时0秒，协议版本V2.2，固件版本V2.2。
---

### 4.5 0xF4 - Flash数据回传
用于回复 Flash 读取请求。

**Payload 结构（4 Bytes）**
| Byte 0     | Byte 1     | Byte 2     | Byte 3     |
| ---------- | ---------- | ---------- | ---------- |
| **ADDR_H** | **ADDR_L** | **Data_H** | **Data_L** |

**参数说明**
| 参数     | 类型     | 说明              |
| -------- | -------- | ----------------- |
| **ADDR** | `uint16` | 读取的 Flash 地址 |
| **Data** | `uint16` | 读取的 Flash 数据 |

---

### 4.6 0xF6 - 六轴IMU数据回传
用于回复六轴IMU查询请求（`0xC6`）或自动回传设置中的IMU回传项。

**Payload 结构（12 Bytes）**

| Byte 0   | Byte 1   | Byte 2   | Byte 3   | Byte 4   | Byte 5   | Byte 6   | Byte 7   | Byte 8   | Byte 9   | Byte 10  | Byte 11  |
| -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- |
| **Ax_H** | **Ax_L** | **Ay_H** | **Ay_L** | **Az_H** | **Az_L** | **Gx_H** | **Gx_L** | **Gy_H** | **Gy_L** | **Gz_H** | **Gz_L** |

**参数说明**

| 参数   | 类型    | 说明                                      |
| ------ | ------- | ----------------------------------------- |
| **Ax** | `int16` | X轴加速度，单位由传感器配置决定（如 mg）  |
| **Ay** | `int16` | Y轴加速度，单位由传感器配置决定（如 mg）  |
| **Az** | `int16` | Z轴加速度，单位由传感器配置决定（如 mg）  |
| **Gx** | `int16` | X轴角速度，单位由传感器配置决定（如 dps） |
| **Gy** | `int16` | Y轴角速度，单位由传感器配置决定（如 dps） |
| **Gz** | `int16` | Z轴角速度，单位由传感器配置决定（如 dps） |

* **返回示例** (ID=0xC32B): `AA 66 C3 2B 0E F6 00 64 FF 9C 03 E8 00 0A FF F6 00 00 0D` 表示加速度与角速度均按 `int16` 大端解析。

---

### 4.7 0xF7 - 姿态数据回传
用于回复姿态数据查询请求（`0xC7`）或自动回传设置中的姿态回传项。

**Payload 结构（6 Bytes）**
| Byte 0      | Byte 1      | Byte 2     | Byte 3     | Byte 4    | Byte 5    |
| ----------- | ----------- | ---------- | ---------- | --------- | --------- |
| **Pitch_H** | **Pitch_L** | **Roll_H** | **Roll_L** | **Yaw_H** | **Yaw_L** |

**参数说明**
| 参数      | 类型    | 说明                                  |
| --------- | ------- | ------------------------------------- |
| **Pitch** | `int16` | 俯仰角，单位0.1度，正数表示向下       |
| **Roll**  | `int16` | 横滚角，单位0.1度，正数表示右倾       |
| **Yaw**   | `int16` | 偏航角，单位0.1度，正数表示顺时针旋转 |

* **返回示例** (ID=0xC32B): `AA 66 C3 2B 06 F7 00 00 00 00 00 00 D5` 表示姿态数据均为0。
* **返回示例** (ID=0xC32B): `AA 66 C3 2B 06 F7 FF 9C 00 64 FF 38 15` 表示Pitch=-10.0度，Roll=10.0度，Yaw=-20.0度。

---

### 4.8 0xF8 - 深度数据回传
用于回复深度数据查询请求（`0xC8`）或自动回传设置中的深度回传项。

**Payload 结构（2 Bytes）**
| Byte 0      | Byte 1      |
| ----------- | ----------- |
| **Depth_H** | **Depth_L** |

**参数说明**
| 参数      | 类型     | 说明                                  |
| --------- | -------- | ------------------------------------- |
| **Depth** | `uint16` | 深度数据，单位0.1米，例：`23` = 2.3米 |

* **返回示例** (ID=0xC32B): `AA 66 C3 2B 02 F8 00 17 C9` 表示深度2.3米。
* 
---

## 5. Flash 内存映射 (STM32)

使用 STM32 内部 Flash，按 16-bit 半字读写。

| Offset   | Bit [15:8]     | Bit [7:0]      | 说明                           | 默认值 |
| -------- | -------------- | -------------- | ------------------------------ | ------ |
| **0x00** | Fish ID (H)    | Fish ID (L)    | 设备唯一 ID                    | -      |
| **0x02** | Password (H)   | Password (L)   | 配对密码                       | 0x0000 |
| **0x04** | Lora Channel   | Lora Init      | LoRa 信道号、Lora初始化标志    | 0x1700 |
| **0x06** | Boot Mode      | Reply Enable   | 开机模式、回复使能             | 0x0000 |
| **0x08** | OC Time        | Servo Bad      | 过流超时时间(s)、舵机损坏标志  | 0x0200 |
| **0x0A** | CPG Amp (H)    | CPG Amp (L)    | CPG 摆幅                       | 0x0000 |
| **0x0C** | CPG Bias (H)   | CPG Bias (L)   | CPG 偏置                       | 0x0000 |
| **0x0E** | CPG Freq       | Intv Nburst    | CPG 频率、brust次数            | 0x0000 |
| **0x10** | Intv Coast (H) | Intv Coast (L) | 滑行时间(x0.1s)                | 0x0000 |
| **0x12** | Speed Gear     | Turn Gear      | 速度档位、转向档位             | 0x0000 |
| **0x14** | Servo1 Bias(H) | Servo1 Bias(L) | int16 舵机1 机械安装偏差       | 0x0000 |
| **0x16** | Servo2 Bias(H) | Servo2 Bias(L) | int16 舵机2 机械安装偏差       | 0x0000 |
| **0x18** | PlayIndex      | Reserved       | 表演模式序号                   | 0x0000 |
| **0x1A** | Host ID (H)    | Host ID (L)    | 上位机ID（定点上行用途）       | 0x0001 |
| **0x1C** | Pair Enable    |                | 上位机ID使能标志（不参与鉴权） | 0x0000 |

---

## 6. 附录

### 错误代码表

| 代码  | 含义       | 备注                          |
| ----- | ---------- | ----------------------------- |
| **0** | 无错误     |                               |
| **1** | 无效指令   | CMD 不存在                    |
| **2** | 参数异常   | Payload 数据越界              |
| **3** | 密码错误   | 鉴权失败                      |
| **4** | 电量不足   | **强制回复**                  |
| **5** | 舵机异常   | 过流持续一定时间 **强制回复** |
| **6** | 浸水告警   | PCB浸水检测触发 **强制回复**  |
| **7** | 传感器异常 | IMU或深度传感器数据异常       |

:::details

:::detailsSummary
更新日志
:::

:::detailsContent
2025年7月20日，初稿
2025年7月26日，增加返回部分协议
2025年7月28日，增加搜索附近设备、开关协议
2025年9月7日，完善逻辑，增加返回状态，表演模式，合并电源开关，增加回复开关
2025年9月23日，修改mW单位错误，舵机位置控制(双)的AA进行了改动
2025年10月10日，修复CPG参数中偏置无方向问题
2025年11月2日，增加开机模式选择
2025年12月2日，增加开机模式单独配置；完善EEPROM；增加LED灯模式；增加强制修改ID；对“返回状态”进行重新定义，不兼容V1.4及以前的协议
2026年1月15日，更新为V2.0，可变包长度，新增MarkDown格式
2026年1月19日，更新V2.0文档，修正部分错误
2026年2月22日，更新至V2.1，增加配对逻辑，删除MOS温度相关逻辑/功能。增加PCB浸水错误代码。预留六轴IMU接口。预留电调PWM接口。预留第三个舵机PWM接口。预留深度传感器接口。
2026年3月2日，合并查询指令 `0xC3/0xC5` 为 `0xC3`，合并返回帧 `0xF3/0xF5` 为 `0xF3`（在 `0xF3` 末尾增加 `TCP_Ver`、`SW_Ver`）。
2026年3月2日，协议升级到 V2.2；整理 `0xD*`，将 `0xD1/0xD2/0xD3/0xD5` 合并为统一的 `0xD1` 开机参数设置。
:::

:::


